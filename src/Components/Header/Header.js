import React from "react";
import "./Header.css";

function Header(){
    return <header className ="header">
    <h1> Clicky Game</h1>
    <h2>Click on the flower to earn points, but don't click the same image more than once!</h2>
    </header>
}

export default Header;